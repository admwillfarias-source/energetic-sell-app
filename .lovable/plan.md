## Auditoria de Performance — AWR Baterias\n\nAnálise feita com base em `browser--performance_profile`, network logs ao vivo, leitura de `BatteryGrid`, `BestSellers`, `catalogStore`, `loadTracking`, `index.html` e inventário de assets.\n\n---\n\n### 1. `fitments` — 1601 linhas / ~390 KB JSON baixadas em toda visita ⚠️ ALTO IMPACTO\n\n**O que acontece hoje:** `ensureCatalogLoaded()` faz `SELECT *` paginado (2 requests de 1000 linhas) na tabela `fitments` toda vez que o usuário busca por veículo. Cada linha tem 11 colunas (várias só usadas no admin: `id`, `created_at`, `updated_at`, `code` redundante).\n\n**Problemas:**\n- Sem cache HTTP (cada navegação refaz fetch).\n- Colunas desnecessárias no payload.\n- Carregado eager por `HeroSection` em idle, mesmo se o usuário nunca buscar por veículo.\n\n**Correções:**\n- `select(\